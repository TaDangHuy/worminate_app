import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#3b8767",
    },
    secondary: {
      main: "#009688",
    },
  },
  typography: {
    fontFamily: "Nunito",
    fontSize: 16,
  },
});
