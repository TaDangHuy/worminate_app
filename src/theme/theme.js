import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#009688",
    },
    secondary: {
      main: "#009650",
    },
  },
  typography: {
    fontFamily: "Itim",
    fontSize: 16,
  },
});
